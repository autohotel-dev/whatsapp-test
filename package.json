{
  "name": "whatsapp-webhook",
  "version": "1.0.0",
  "description": "Webhook server for WhatsApp Business API",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "dev": "node app.js",
    "test": "node test-webhook.js",
    "build": "echo 'Iniciando build...' && npm install && echo 'Build completado exitosamente'"
  },
  "dependencies": {
    "axios": "^1.13.1",
    "crypto-js": "^4.2.0",
    "dotenv": "^17.2.3",
    "express": "^5.1.0",
    "selfsigned": "^4.0.0"
  },
  "engines": {
    "node": ">=14.0.0"
  },
  "keywords": [
    "whatsapp",
    "webhook",
    "api"
  ]
}
